/**
 * Document uploads have been completely disabled and removed from the system.
 * This file is kept as a placeholder to prevent routing import errors.
 * All document-related functionality has been removed from:
 * - Routes (App.tsx)
 * - tRPC routers (server/routers.ts)
 * - API endpoints (server/_core/index.ts)
 * - UI Navigation (DashboardLayout.tsx)
 */

export default function AdminDocuments() {
  return null;
}
